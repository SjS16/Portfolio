<div class="container portfolio-container">
  <div class="row">
    <div class="col-md-7">
    <%= image_tag @portfolio_item.main_image.to_s, width: '100%' unless @portfolio_item.main_image.nil? %>
    </div>
    <div class="col-md-5">
      <h1><%= @portfolio_item.title %></h1><%= link_to "Edit", edit_portfolio_path(@portfolio_item)  if logged_in?(:site_admin) %>
      <hr>
      <h4><em><%= @portfolio_item.subtitle %></em></h4>
      <p><%= @portfolio_item.body %></p>
      <hr>
      <h3>Technologies used:</h3>

      <% @portfolio_item.technologies.each do |t|%>
        <p><%= t.name %></p>
      <% end %>
    </div>
  </div>
</div>



