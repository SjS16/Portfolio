<% if logged_in?(:site_admin) %>
  <p class="blog-post-meta breadcrumb"><%= set_status blog %> |
  <%= set_featured blog %> |
  <%= link_to 'Edit', edit_blog_path(blog) %> |
  <%= link_to 'Delete', blog, method: :delete, data: { confirm: 'Are you sure?' } %></p>
<% end%>